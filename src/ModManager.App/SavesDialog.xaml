<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="ModManager.App.SavesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModManager.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Save snapshots"
    CloseButtonText="Close"
    DefaultButton="Close">

    <StackPanel Spacing="12" Width="480">
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FolderBox" Grid.Column="0" Header="Save folder" IsReadOnly="True"
                     PlaceholderText="Set the game's save folder to enable snapshots" />
            <Button Grid.Column="1" Content="Change…" VerticalAlignment="Bottom" Click="OnChangeFolder" />
        </Grid>

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="LabelBox" Grid.Column="0" Header="Label (optional)" PlaceholderText="e.g. before raid" />
            <Button x:Name="BackupButton" Grid.Column="1" Content="Back up now" VerticalAlignment="Bottom" Click="OnBackup" />
        </Grid>

        <TextBlock Text="Snapshots" FontWeight="SemiBold" />
        <ListView x:Name="SnapshotList" MaxHeight="260" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:SaveRow">
                    <Grid ColumnSpacing="8" Padding="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind Detail}" Opacity="0.6" FontSize="12" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Restore" Click="OnRestore" VerticalAlignment="Center" />
                        <Button Grid.Column="2" Click="OnDelete" VerticalAlignment="Center"
                                Background="Transparent" BorderThickness="0" ToolTipService.ToolTip="Delete snapshot">
                            <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="{StaticResource ThemeDanger}" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock x:Name="EmptyText" Text="No snapshots yet." Opacity="0.6" Visibility="Collapsed" />
        <TextBlock x:Name="StatusText" Opacity="0.75" FontSize="12" TextWrapping="Wrap" />
    </StackPanel>
</ContentDialog>
