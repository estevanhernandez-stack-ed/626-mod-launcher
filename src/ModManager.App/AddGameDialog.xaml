<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="ModManager.App.AddGameDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add a game"
    PrimaryButtonText="Add"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimary">

    <StackPanel Spacing="12" Width="440">
        <Expander x:Name="AiExpander" HorizontalAlignment="Stretch" Header="Add with AI">
            <StackPanel Spacing="8" Width="400">
                <TextBlock TextWrapping="Wrap" Opacity="0.75" FontSize="12"
                           Text="Type the game name, copy the prompt into any AI agent, paste the JSON it returns, then Apply to pre-fill the wizard." />
                <TextBox x:Name="AiGameNameBox" Header="Game name" PlaceholderText="e.g. Elden Ring" />
                <Button Content="Copy prompt" Click="OnCopyProfilePrompt" />
                <TextBox x:Name="AiJsonBox" Header="Agent JSON" AcceptsReturn="True" Height="150" TextWrapping="Wrap"
                         PlaceholderText="Paste the JSON the agent returned" />
                <Button Content="Apply profile" Click="OnApplyProfile" />
                <TextBlock x:Name="ProfileStatus" Visibility="Collapsed" TextWrapping="Wrap" FontSize="12" />
            </StackPanel>
        </Expander>
        <Expander x:Name="BatchExpander" HorizontalAlignment="Stretch" Header="Batch from Steam (multiple games)">
            <StackPanel Spacing="8" Width="400">
                <TextBlock TextWrapping="Wrap" Opacity="0.75" FontSize="12"
                           Text="Pick the Steam games you want to onboard, copy the batch prompt into your agent, paste the JSON array back, then Apply all. Profiles that pass register on Add; the rest are skipped." />
                <ListView x:Name="BatchSteamList" SelectionMode="Multiple" Height="160"
                          BorderBrush="{ThemeResource ControlElevationBorderBrush}" BorderThickness="1">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button Content="Copy batch prompt" Click="OnCopyBatchPrompt" />
                <TextBox x:Name="BatchJsonBox" Header="Agent JSON array" AcceptsReturn="True" Height="120" TextWrapping="Wrap"
                         PlaceholderText="Paste the JSON array the agent returned" />
                <Button Content="Apply all" Click="OnApplyBatch" />
                <TextBlock x:Name="BatchStatus" Visibility="Collapsed" TextWrapping="Wrap" FontSize="12" />
                <ItemsControl x:Name="BatchResultsList" Margin="0,4,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{ThemeResource ControlElevationBorderBrush}" BorderThickness="1"
                                    CornerRadius="4" Padding="8" Margin="0,0,0,4">
                                <StackPanel Spacing="2">
                                    <TextBlock Text="{Binding Headline}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Detail}" Opacity="0.75" FontSize="12" TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Expander>
        <ComboBox x:Name="PopularGamesBox" Header="Popular game (optional)" HorizontalAlignment="Stretch"
                  DisplayMemberPath="Name" PlaceholderText="Pick a known moddable game"
                  SelectionChanged="OnPopularSelected" />
        <StackPanel Spacing="6">
            <TextBlock Text="Quick add from Steam" FontWeight="SemiBold" FontSize="13" />
            <TextBlock Text="Check the games to add — engine, folder, and saves are detected for you. Then click Add."
                       TextWrapping="Wrap" Opacity="0.75" FontSize="12" />
            <ListView x:Name="SteamGamesList" SelectionMode="Multiple" MaxHeight="180"
                      DisplayMemberPath="Display"
                      BorderBrush="{ThemeResource ControlElevationBorderBrush}" BorderThickness="1"
                      SelectionChanged="OnSteamSelectionChanged" />
            <TextBlock x:Name="SteamEmptyNote" Visibility="Collapsed"
                       Text="No installed Steam games detected." Opacity="0.7" FontSize="12" />
            <TextBlock x:Name="SteamManualNote" Visibility="Collapsed"
                       TextWrapping="Wrap" Opacity="0.7" FontSize="12" />
            <TextBlock x:Name="SteamSelectionStatus" Visibility="Collapsed" FontSize="12" />
        </StackPanel>
        <TextBox x:Name="NameBox" Header="Game name" PlaceholderText="e.g. Windrose" />
        <StackPanel Spacing="2">
            <ComboBox x:Name="EngineBox" Header="Engine" HorizontalAlignment="Stretch"
                      DisplayMemberPath="Label" PlaceholderText="Select engine…"
                      SelectionChanged="OnEngineChanged" />
            <TextBlock x:Name="EngineHint" Foreground="{StaticResource ThemeAccent}" FontSize="12"
                       Visibility="Collapsed" Text="Auto-detected from the game folder — change if it's wrong." />
        </StackPanel>
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FolderBox" Grid.Column="0" Header="Game folder"
                     PlaceholderText="C:\...\steamapps\common\YourGame" />
            <Button x:Name="BrowseButton" Grid.Column="1" Content="Browse" VerticalAlignment="Bottom" Click="OnBrowse" />
        </Grid>
        <TextBox x:Name="ModPathBox" Header="Mod folder (relative to the game folder)" />
        <TextBox x:Name="SteamBox" Header="Steam App ID (optional — enables Launch)" />
        <ComboBox x:Name="SaveRootBox" Header="Save root (optional)" HorizontalAlignment="Stretch"
                  PlaceholderText="Where saves live" />
        <TextBox x:Name="SaveSubPathBox" Header="Save subfolder (relative to the save root, optional)" />
        <TextBox x:Name="RequiredLauncherBox" Header="Required launcher (relative exe, optional)" />
        <TextBlock x:Name="ErrorText" Foreground="{StaticResource ThemeDanger}" Visibility="Collapsed"
                   Text="Name, game folder, and engine are required." TextWrapping="Wrap" />
    </StackPanel>
</ContentDialog>
