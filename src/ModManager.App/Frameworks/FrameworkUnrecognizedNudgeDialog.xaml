<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="ModManager.App.Frameworks.FrameworkUnrecognizedNudgeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="Continue as mod"
    SecondaryButtonText="Open feedback link"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    AutomationProperties.Name="Looks like a framework">

    <!-- Dialog shell (vibe-glow F-008): 3px accent rail + mono-caps stencil eyebrow + title,
         per the design language's dialog rules. Rail bleeds toward the dialog edges. -->
    <ContentDialog.Title>
        <StackPanel Spacing="6">
            <Border Height="3" Background="{StaticResource ThemeAccent}" Margin="-24,0,-24,4"
                    AutomationProperties.AccessibilityView="Raw" />
            <TextBlock Text="FRAMEWORK // UNRECOGNIZED" FontFamily="{StaticResource MonoFontFamily}" AutomationProperties.AccessibilityView="Raw"
                       FontSize="{StaticResource TagFontSize}" CharacterSpacing="80"
                       Foreground="{StaticResource ThemeInkDim}" />
            <TextBlock Text="Looks like a framework" FontSize="20" FontWeight="SemiBold" />
        </StackPanel>
    </ContentDialog.Title>

    <!-- Shown when a dropped FromSoft zip has a DLL proxy at its root (dinput8/version/winhttp)
         but doesn't match any KnownFramework catalog entry. Lets the user open a feedback link
         so we can grow the catalog over time. -->
    <StackPanel Spacing="12" Width="440">
        <TextBlock TextWrapping="Wrap">
            This zip looks like a game framework or loader (it has a proxy DLL at its root),
            but it isn't in our catalog yet. We can't safely auto-install frameworks we don't
            recognize — but if you've used this one before, we'd love to add it.
        </TextBlock>
        <TextBlock TextWrapping="Wrap" Foreground="{StaticResource ThemeInkSoft}" FontSize="12">
            If you want to install it anyway, the launcher will route it through regular mod
            intake (extracts to your mods folder). This usually doesn't work for frameworks
            but is sometimes correct.
        </TextBlock>
        <TextBlock x:Name="FilenameText" Foreground="{StaticResource ThemeInkDim}" FontFamily="{StaticResource MonoFontFamily}" FontSize="11" />
    </StackPanel>
</ContentDialog>
