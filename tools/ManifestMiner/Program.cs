// Placeholder entry point — the real CLI (fetch -> parse -> normalize -> validate -> emit)
// lands in Task 4. An Exe-typed project needs a Main so the tool + the test project that
// references it both build; this single statement satisfies that until Task 4 replaces it.
Console.WriteLine("ManifestMiner: CLI not yet implemented (skeleton).");
