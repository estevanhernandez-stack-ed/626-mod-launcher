# Backlog — captured 2026-08-05

- **Status:** Captured live during the `feat/discovery-sweep` smoke session. Nothing here is
  scheduled; each item gets its own spec → plan → build when it comes up the queue.
- **Sizing:** `S` one sitting · `M` a focused build · `L` its own multi-day feature.
- **Origin:** Windrose + Cyberpunk 2077 live smoke. B1 is the headline — it was found by chasing
  why a 194-mod Cyberpunk install was invisible to the launcher.

## A. Correctness — found by smoke, real today

### A1. Registered games never pick up corrected manifest facts · `M` · **DONE 2026-08-09**

**Problem.** `GameEntry.FileExtensions` (and its siblings) are written **once, at registration**
(`EnginePresets.cs:86` — `input.FileExtensions ?? preset.FileExtensions`) and frozen into
`games.json`. `EffectiveManifest` merges remote-over-embedded for the *manifest*, but nothing ever
re-applies a corrected definition to a game the user already added.

**Observed.** Este's Cyberpunk 2077 registered with `fileExtensions: ["pak"]` while the manifest
says `["archive"]`. `Scanner` builds `ctx.FileRe` from the registered value, so **194 `.archive`
mods on disk showed as zero mods in the launcher** — and the discovery sweep was blind for the same
reason (`DiscoverySweepOptions.EngineExtensions` is `ctx.Game.FileExtensions`). Repaired by hand on
2026-08-05 (backup at `games.json.bak-20260805-080157`).

**Why it matters beyond one game.** This silently voids the manifest feed's core promise — *"a new
game on a known engine needs no app release."* Every definition correction we mine only reaches
users who had NOT already added that game. The longer a user has had the launcher, the less the
feed helps them.

**Proposed fix.** At context-build time (in memory — never rewrite `games.json`), prefer the
manifest's value when the registered value still equals the **engine-preset default** and the
manifest carries a specific one. A user who genuinely customized a field keeps it; a user carrying
a stale preset snapshot gets the correction. Changes only what the scanner *looks for* — no file
op, so it stays inside the reversibility law.

**Open Q — resolved by measurement.** Preset-default equality was the proposed heuristic for
"untouched", with a `userSet` marker as the exact-but-needs-migration alternative. Checked against
every real registration on the dev machine, the heuristic separates them correctly:

| game | stored | preset default | verdict |
|---|---|---|---|
| cyberpunk-2077 | `["pak"]` | custom → `["pak"]` | matches → stale, corrected to `["archive"]` |
| marvel-s-spider-man-2 | `[".smpcmod", …]` | custom → `["pak"]` | differs → user's, kept |
| windrose | `["pak","ucas","utoc"]` | ue-pak → same | matches, manifest silent → kept |

So the marker (and its migration) buys nothing today. `RegistrationRefresh` in Core carries the rule
with tests; order and case do not count as customisation (they are not choices a user makes), but a
SUPERSET of the preset default does. Applied at context-build time only — `games.json` is never
rewritten, so what the user chose stays visible and editable. Verified end to end: the exact stale
Cyberpunk shape goes from 0 mods visible to 2, while a deliberately-chosen extension list is left
alone. Covers `GroupingRule` as well, which froze the same way.

### A4. An agent could not tell "managed" from "organized" · `M` · **DONE 2026-08-09**

Found by failing at it. Asked whether Elden Ring's mods had been reorganized, the agent ran a dozen
filesystem probes and reached a confidently WRONG answer twice — first "626 shows zero mods for Elden
Ring, for everyone", then a proposal to delete a Vortex staging folder as redundant. The launcher had
the correct answer on screen the whole time ("Detected 11 mods — loose-file install, no Mod Engine 2
needed"). The agent surface could not be asked.

Three things made the wrong answer easy to reach, all now fixed:

- `get_mod_context` reported the DECLARED mod location with no reality check. Elden Ring declares a
  Mod Engine 2 `mod` folder that does not exist; every mod loads by direct-inject under `Game\`.
  Declared-as-fact reads as a broken install. Now carries `exists` + `absolute` per location.
- `location` on a mod is the listing LANE (`direct-inject`), not a place — so nothing could say
  whether content sat in `Game\mods\` or loose in `Game\`. `files` and `isLoader` were on the
  record all along and simply never surfaced; both are now exposed.
- `mod` vs `mods` are different folders, and a truncated directory listing hid the second. Containment
  now tests on a separator boundary, with a test pinning that pair specifically.

`GameShape` (`src/ModManager.Core/Discovery/GameShape.cs`) puts declared and actual side by side:
mechanism, play folder, declared locations with `exists`, the real directories mods were found in,
loaders present, an alignment verdict, and plain-language notes. Exposed as `get_game_shape`.

**The design call worth keeping.** "Organized" was the obvious framing and it is wrong. A
direct-inject mod is DEFINED by living loose at the play-folder root — `dinput8.dll` is a loader,
`reshade-shaders` is read from the root by name. A tidiness verdict would push a user to "fix" a
working install by breaking it. So the report never judges tidiness; it reports ALIGNMENT — were the
mods found where the registration said to look — and states outright that drift is a description, not
a defect. Live results: Elden Ring `Drifted` (healthy, mechanism the registration never mentions),
Cyberpunk `Aligned`.

Read-only, like every discovery surface. `.mcp.json` now wires the server, which was built but
unreachable — `{"mcpServers": {}}`.

### A5. The discovery sweep and the mod-key formula can disagree after a manifest refresh · `S` · **DONE 2026-08-09**

Found by the whole-branch review of the registration-repair work, reading this branch against the
commit before it. Pre-existing on master since A1 landed (`2e0b36a`) — not introduced by that branch,
but made reachable by it.

`MainViewModel.cs:2700-2703` carries the comment *"ctx.Game.FileExtensions is the exact value
ctx.FileRe (and therefore Scanner.ModKeyFor) is built from — using anything else would let the sweep
and the key formula disagree."* That stopped being true when `RegistrationRefresh` landed: `ctx.FileRe`
is now built from `declaredExts`, which may be the MANIFEST's list rather than the stored one. Same
shape at `Scanner.cs:1408` and `Scanner.cs:1514`, which branch on `c.Game.FileExtensions.Count > 0`
rather than on `c.Exts`.

So for exactly the case A1 exists to fix — a stale `["pak"]` corrected to `["archive"]` — the sweep and
the key formula *do* disagree. Fix is to read `c.Exts` (the resolved value) at all three sites and
correct the comment. Cheap; needs a test that a refreshed registration produces the same keys through
both paths.

**Fixed 2026-08-09.** `GameContext` now carries `DeclaredExts` — the resolved-but-unnormalised list,
assigned from the same local `ctx.FileRe` is built from — and all three sites read it.

One of the three was **live**. `DiscoverySweepOptions.EngineExtensions` was the raw stored value, so on
a stale Cyberpunk registration the scanner listed all 194 `.archive` mods while `IsEngineShaped` refused
to call a single one of them engine-shaped: the launcher showed the mods and "Identify my mods" could
not see them. The other two — the `Count > 0` branches in `Md5IdentifyArchivesAsync` and
`ArchiveModKeysFor` that pick extension-based vs catalog-based keying — were **latent**. They ask only
whether the list is empty, and `cyberpunk-2077` is the only manifest entry carrying `fileExtensions`
today, on the `custom` preset whose default `["pak"]` is already non-empty. Stored and resolved agree on
emptiness for every registration that exists right now. They diverge the moment the feed gives
extensions to a game whose preset default is empty (`smapi`, `fromsoft`, `decima`) — which needs no app
release, so it is a data change away.

**The proposed fix above was wrong and is left as written.** `c.Exts` was `DeclaredExts` *plus*
empty→`["pak"]` substitution, lowercasing, dot-trimming, and `Regex.Escape`. Using it would have made
the sweep fire on `.pak` for the three folder-based engines that genuinely have no extensions, and the
escaping broke a plain `Contains` for any extension holding a regex metacharacter. The three values are
easy to confuse, which is why `DeclaredExts` ships with a doc comment naming how it differs from both
neighbours. Four tests in `tests/ModManager.Tests/GameContextDeclaredExtsTests.cs`: the live bug, the
emptiness invariant the two Scanner branches rely on, a guard proving the two can diverge (injected via
`EffectiveManifest.SetRemote`), and the substitution difference. (A7 below has since moved the
`Regex.Escape` out of `Exts` and into the `FileRe` interpolation, so only the substitution now separates
the two lists — the reason the sweep must still read `DeclaredExts` is unchanged.)

### A7. Intake classified a regex-metacharacter extension as "skip" · `S` · **DONE 2026-08-09**

Found by the A5 review, reading `GameContext.Exts`'s own doc against its callers.

`ctx.Exts` was `Regex.Escape`d, and seven intake sites passed it to `Intake.ClassifyDrop`, which
compares by literal equality (`Intake.cs:15-23`). A game declaring `mod+pak` got
`Exts = ["mod\+pak"]`, so a real `foo.mod+pak` classified as **skip** — listed by the scanner (whose
regex escapes correctly), refused by intake. Reachable through every one of the seven: loose drop,
zip entry, dropped-folder walk, `ArchiveModKeysFor`, and readme capture. Confirmed by failing test,
not by reading.

**The prescription first filed with this entry was wrong, and so was the doc it came from.** Both
said the fix needed a third list or a `ClassifyDrop` flag, because *"those sites depend on the
empty→`["pak"]` substitution … empty is meaningful — it is how a folder-based engine is told apart
from an extension-based one."* The second half is true of the unsubstituted list; it is not why the
intake sites need the substitution. They need it because **an extension-less registration is a pak
game to every other reader**: `GameEntry.FileExtensions` defaults to empty, so any hand-written or
quick-added entry that omits the field gets `FileRe = \.(pak)$`, `ModKeyFor` stripping `.pak`, and
the scanner listing lane showing `.pak` files as mods. Moving intake alone onto the unsubstituted
list would have made intake the one reader that disagrees — a mod visible in the list that cannot
be added.

The folder-based engines the entry invoked neither want the substitution nor depend on it:
`fromsoft` never reaches these sites (the App routes it through `DirectInjectService`, and
`ArchiveModKeysFor` / `Md5IdentifyArchivesAsync` branch to `DirectInject.MatchSignaturesInZip`), and
`decima` early-returns to `LooseRootIntake.Plan`. So the substitution was worth keeping, for a
different reason than the one given.

**Fixed** by making the escaping local to the regex rather than adding a third list: `ctx.Exts` now
carries the extensions themselves (lowercased, dot-stripped, empty→`["pak"]`), and
`Scanner.GameContext` applies `Regex.Escape` at the point it interpolates them into `FileRe`.
Nothing else in the codebase read `Exts` — the escaped spelling had exactly one consumer group, and
that group compared it literally. `FileRe` is byte-identical for every input, pinned by test. Two
values in `GameContext`, not three.

Left open, separately: `smapi` games do fall through to `Scanner.PlanIntake` with the substituted
`["pak"]`, so dropping an archive that happens to contain a stray `.pak` copies it flat into `Mods/`
where SMAPI cannot load it. That is not the escaping bug — SMAPI intake (folder-shaped mods carrying
a `manifest.json`) is simply not implemented, and narrowing the substitution would not implement it.

### A2. No in-app way to repair a game registration · `S` · **DONE 2026-08-09**

There is no edit-game dialog — `AddGameDialog` is add-only. A user whose registration is wrong (A1)
has no route out except removing and re-adding the game, which risks their per-game state. Even
after A1 lands, an explicit "edit this game's definition" surface is the honest escape hatch.

**Core primitives landed 2026-08-09** — `userSet` marker (`GameEntry.UserSet` + `RegistrationRefresh`
precedence), `DataDirMove` (Plan/Execute), `RegistrationChange` (the planner). Spec:
`docs/superpowers/specs/2026-08-09-registration-repair-core-design.md`. Plan:
`docs/superpowers/plans/2026-08-09-registration-repair-core.md`. **UI landed 2026-08-09** — `Check
setup…` plus a conditional banner, one `GAME // SETUP` dialog (diagnosis from `GameShape`, eight
fields behind an expander, live consequences), a move-or-pin confirm, and
`RegistrationRepairService`. Spec: `docs/superpowers/specs/2026-08-09-registration-repair-ui-design.md`.
Plan: `docs/superpowers/plans/2026-08-09-registration-repair-ui.md`. Smoke steps in
`docs/smoke-tests/pending.md`. **A2 is closed.**

### A8. `get_game_shape` does not expose the declared-vs-derived flag · `XS`

`GameShape.DeclaredLocation` gained a `Declared` flag (PR #266) so a launcher-appended location — the
synthetic `ue4ss-mods` entry `Scanner.GameContext` adds when the launcher owns a UE4SS install — is
not presented as something the registration declares. The dialog renders it distinctly.

The MCP projection did not follow. `src/ModManager.Mcp/Tools/ModTools.cs` still emits
`{name, path, absolute, exists}`, so for a derived entry `path` silently changed meaning (a bare
label became an absolute path) with no flag to explain it. `GameShape`'s own doc claims "every reader
renders them as what they are" — this reader does not.

One line: add `declared = d.Declared` to the projection. Worth doing because that tool is how an agent
asks the launcher about a registration instead of guessing from the filesystem, and this is exactly the
class of confusion A5 and A7 were about.

### A9. SMAPI intake places a stray `.pak` flat in `Mods/` · `S`

Found while verifying A7's premise, and deliberately not folded into that fix.

`smapi` has no `loose-root` form and is not direct-inject-backed, so it reaches `Scanner.PlanIntake`
with the empty→`["pak"]` substitution applied — meaning a `.pak` inside a dropped archive classifies
as a mod for a Stardew game and lands flat in `Mods/`. A SMAPI mod is a FOLDER containing a
`manifest.json`, never a loose pak.

This is not a list change. Fixing it by taking the substitution away from intake would make intake
refuse a mod the scanner listing lane already shows (see A7's verdict — an extension-less registration
is a pak game to `FileRe` and `ModKey`). It needs folder-shaped SMAPI intake: recognise the
`manifest.json` marker and place the containing folder, the way the framework installers already do.

### A10. The updates list renders a version pair it cannot fill · `S` · Este, 2026-08-17

Found while shooting Store screenshots — the second time now that the shop window has surfaced a
defect nobody had filed. Shot 4 of the listing set is the updates view, and roughly twenty
consecutive rows read **`unknown -> 1.2.1`**. Others read **`1.1 -> 1`**, **`1.0.1 -> 1.0.0`** and
**`1 -> 1.0`** — two apparent downgrades and a no-op, all presented as updates. Header said
*115 updates*.

**Do not "fix" `Mod.UpdateAvailable`.** That precedence is deliberate and the reasoning is written
out at `src/ModManager.Core/Mod.cs:62-87`: an unknown installed version already refuses to claim an
update, and the clause exists because a 98-mod live smoke lit the UPDATE chip on every identified row
at once. The comment's own warning is the thing to keep in mind here — *"a false positive shaped like
the truth never gets reported."*

The actual shape: `NexusUpdateAvailable ?? (version compare)`. Nexus's per-user flag **outranks** the
compare, and rightly so — Nexus knows which file the user downloaded and we often do not. So a row
can be listed entirely on Nexus's authority while we hold no installed version at all. The listing
then renders `installed -> latest` regardless, and prints `unknown` on the left.

So this is presentational, not a correctness bug in the decision:

- When the listing reason is the Nexus flag rather than a version difference, say that
  (*"Nexus says you're behind"*) instead of printing a comparison we cannot stand behind.
- Never render a pair that reads as a downgrade or a no-op. `1.1 -> 1` and `1.0.1 -> 1.0.0` are worse
  than showing nothing — they invite the user to "update" backwards.
- Reconsider what the count means. *115 updates* is counting rows we cannot substantiate, and the
  badge is load-bearing on the library home.

Where: `src/ModManager.Core/ModUpdateSummary.cs` mirrors the same three inputs and drives both the
badge and this view; the row rendering is the updates view in the App layer.

### A11. One multi-option mod becomes several identical update rows · `M` · Este, 2026-08-17

Same screenshot. **"Faster Ships" appears four times** in the 0.17 capture and three times in the
0.18 one, every row identical, every row `1 -> 1.0`.

Root cause is already understood and was recorded in an earlier session: Nexus tracks downloads
**per file**, not per mod, and a multi-option mod publishes several. Faster Ships ships
`FasterShips10`, `FasterShips10_B`, `aaUltraFastShips`, `aaUltraFastShipsB` — those variant chips are
visible on the mod's own row in `docs/store-assets/screenshots-0.17/06-settings.png`. Each tracked
file produces its own update row.

The launcher already understands variant families elsewhere (the chips exist, and C6 taught the name
matcher to refuse a wrong-attach across a variant family). The updates view is where that knowledge
has not landed: group by mod id, show the mod once, and put the file options underneath if they are
worth showing at all.

Related to A10 only by symptom — A10 is what a row says, this is how many rows there are. They will
read as one bug to a user and should probably be fixed together, but they are separate causes.

### A12. The mod count disagrees with itself across three surfaces · `S` · found by the harness, 2026-08-17

The first real smoke-harness run caught this, which is the point of building it. For Windrose:

| Surface | Count |
|---|---|
| Library home row | **30** mods |
| MCP `get_game_shape.modCount` / `list_mods` | **30** |
| Game view (rows + `AppStatusText`) | **27 of 27 enabled** |

The gap is exactly the Faster Ships family. `list_mods` returns `FasterShips10`, `FasterShips10_B`,
`aaUltraFastShips` and `aaUltraFastShipsB` as four entries, all with `displayTitle: "Faster Ships"`;
the game view groups them into one variant row. 30 - 3 = 27. Neither number is wrong on its own
terms - one counts mod keys, the other counts rows a human sees.

**It is a parity violation, though, and against the rule this repo wrote down a day earlier.** Under
*parity, plus the reason* (section E), an agent must see what a human sees. An agent reading the MCP
today confidently reports "you have 30 mods"; the user opens the game, counts 27, and concludes the
agent is wrong. The MCP should expose both - the row count a human would count AND the key count -
with the grouping named, rather than one number that silently means something different from the
screen.

The home row has the same problem without the excuse: it is a human-facing surface showing 30 next to
a game whose own view shows 27.

Related to **A11** and probably one fix: both are the variant-family question, asked once about
update rows and once about counts. Worth doing together.

Also spotted in the same `list_mods` output: **`shared` is listed as a mod** - `"displayTitle":
"Shared (UE4SS internal)"`. That is UE4SS's own internal folder, not a user mod, and it is inflating
every count above. Cheap to exclude; verify against `isLoader` handling first, since something
deliberately named it rather than hiding it.

### A13. Framework detection ORs together two files that are actually AND · `M` · proven live, 2026-08-17

**626 reports a healthy install while the game itself refuses to start its mods.** Proven end to end
on Este's Windrose, not reasoned about.

`FrameworkDeps` documents `DetectRelativePaths` as *"if ANY exists under the resolved candidate roots,
the framework is considered present. Multiple paths cover loader variants."* For UE4SS the two paths
are:

```
Binaries/Win64/ue4ss/UE4SS.dll     <- the runtime
Binaries/Win64/dwmapi.dll          <- the proxy loader that loads it
```

**Those are not variants. They are a chain.** The game loads `dwmapi.dll`, which loads `UE4SS.dll`.
Both are required. OR semantics mean either one alone reads as "framework present".

**The experiment.** Moved `UE4SS.dll` aside, left `dwmapi.dll` in place:

| | |
|---|---|
| 626 status line | `27 of 27 enabled` |
| `NEEDS UE4SS` chips | **0** |
| The game, on launch | **"Failed to load UE4SS.dll"** — its own error dialog |
| `UE4SS.log` | never written. Still Aug 2, still 1074 lines |

Twelve ue4ss-lane mods were dead and the launcher said everything was fine.

**This is the dangerous direction.** A false RED chip is noise; a false GREEN is a user who thinks
their mods are on. Este remembered the opposite symptom — red chip, mods working — which is worth
recording because the real failure is the one nobody notices.

**Same shape elsewhere, unverified but suspicious:** BepInEx lists `BepInEx/core/BepInEx.dll` and
`winhttp.dll` — runtime and proxy, same AND-as-OR. Check every entry before fixing; some lists are
genuinely alternatives and must stay OR. Elden Mod Loader's `dinput8.dll` / `version.dll` /
`winhttp.dll` / `SeamlessCoop/ersc.dll` ARE real proxy-name variants — OR is correct there.

**Shape of the fix:** the record needs to distinguish *any-of* (loader name variants) from *all-of*
(loader plus runtime). Probably two lists rather than one, so the same type expresses both without
the caller guessing which it got.

**Also worth its own look:** the `Refresh` in 626 did not notice either. The check runs on scan and
the file vanished underneath it — defensible, but "Steam updated this game, recheck your mods" is
already a banner this app knows how to show, and a missing framework deserves the same treatment.

### A14. Adoption calls downloaded-but-inactive mods "already installed" · `S` · Este, 2026-08-17

Adding Monster Hunter Wilds raised the adoption dialog against Fluffy Mod Manager's library. Este's
read was right and mine was wrong — I called them "another manager's archive store" and advised
cancelling; they are the user's own mods, downloaded and waiting.

Verified on disk: **13 individual Nexus mods** in `Games/MonsterHunterWilds/Mods/`, each carrying
Nexus's `-<modId>-<version>-<timestamp>` download filename (CatLib 65, Shop Tweaks 30, disable_lens
245, SkipIntroLogos 72, ...). Not a pack — thirteen separate downloads. And **no `natives/` directory
exists in the game folder**, so none of them is deployed. Downloaded, inactive, exactly as Este said.

**So they should be offered, and adopted disabled.** That is already half-true: adoption is
metadata-only (`ApplyDiscoveriesAsync` — *"no file is moved, renamed, or deleted"*), so it cannot
activate anything. The behaviour is fine. The words are not.

**The copy is the bug.** The dialog is headed *"Mods already installed"* and reads *"These look like
mods you already installed by hand."* For this case that is false in the most dangerous direction —
on a game the app itself flags as high ban-risk, it tells the user mods are live when nothing is
deployed. Needs a second framing for downloaded-but-not-deployed: found, not active, adopt to manage.

**That open question is answered, in the code, and it makes this worse.** `MainViewModel.cs:2513`:
the apply distinguishes three reasons a write returns no keys — *"the archive maps to nothing
installed yet / every key was already identified / neither"* — and notes that **the zero-key case is
the downloads-folder NORM**.

So adopting these thirteen zips writes nothing. **Adoption attaches metadata to mods that are already
installed**; nothing here is deployed, so there is nothing to attach to. Este's model was that
cancelling kept them off the list — in fact accepting would not have put them on it either.

That means the dialog is not merely mislabelled. Under a heading that says *"Mods already installed"*
it offers a button reading *"Adopt N mods"* for archives that adoption cannot help with, and the only
honest outcome is a status line explaining that nothing was written. **Adopt is not install.** The
user's actual route to managing these is to deploy them (Fluffy, into `natives/`) and let the scanner
find them, or to drop the zips on 626 and let intake install them.

The fix therefore is not a copy tweak. This surface needs to tell the difference between *"found,
installed, let me name it"* (what adoption is for) and *"found, downloaded, not installed — want me to
install it?"* (what the user has here, and a different action entirely).

**Also a false positive in the same list:** it offered
`Vortex Extension Update - Monster Hunter Wilds Vortex Extension v0.1.4.zip`. That is a Vortex
*extension*, not a game mod. Sitting in the same folder is not enough to make something a mod.

**And the wider gap:** 626 knows Vortex's territory (`VortexTakeover`, `taken-over.json`) and does not
know Fluffy's. Fluffy is the default manager for RE Engine games — Monster Hunter, Resident Evil —
so this will recur for every one of them. Same family as **B3 (adopt another manager's library)**.

What the dialog got right, and should keep: the `dinput8.dll` row reads *"This is the loader other
mods ride on, not a mod itself. Several different loaders ship under this filename, so it can't be
named from the file alone."* Honest about the limit of what a filename can tell you.

### A15. Quick-add takes the mod folder from the engine preset and ignores the feed · `M` · proven live, 2026-08-17

**54 of the 118 feed games with a curated `modPath` would register with the wrong mod folder**, and
nothing corrects it afterwards. Found by adding Monster Hunter Wilds and watching 626 report no mods
for a game with thirteen of them downloaded.

What the feed says, versus what got written:

```
feed        "modPath": "reframework/autorun"     <- and that folder EXISTS on disk
registered  "mods"                               <- does not exist
get_game_shape -> alignment: NoMods, contentRoots: []
```

**Where it goes wrong — `SteamGameImport.Plan`:**

```csharp
var engine  = KnownEngines.ByAppId(game.AppId) ?? folderDetectedEngine;          // manifest IS consulted
var modPath = EnginePresets.Presets.TryGetValue(engine, out var preset)
                  ? preset.ModPath : null;                                        // manifest is NOT
```

The manifest is asked for the engine and then ignored for the path. `custom`'s preset is `"mods"`, so
every curated `modPath` on a `custom`-engine game is discarded at the moment of registration.

**And `RegistrationRefresh` does not clean it up.** It exposes `Extensions` and `Grouping` — there is
no `ModPath`. A1 taught registered games to pick up corrected extensions and grouping; the one field
that decides whether ANY mod is found was left out. So the error is permanent.

**Scale.** 118 feed games carry a curated `modPath`; **54 differ from the preset the add path would
use**. Not edge cases: `cyberpunk-2077` wants `archive/pc/mod`, `dragon-s-dogma-dark-arisen` wants
`nativePC`, `ark-survival-ascended` wants `ShooterGame/Binaries/Win64/ShooterGame/Mods`,
`deep-rock-galactic` wants `FSD/Content/Paks`. Every UE game with a project-name prefix is in here.

**Why it matters more than its size.** The symptom is *"626 found no mods"* on a game the user has
mods for. That does not read as a misconfiguration — it reads as the app not supporting the game.
It is also invisible to every existing test: the registration is internally consistent, the scan
correctly finds nothing in a folder that does not exist, and `get_game_shape` reports it accurately.
Only comparing the registration against the feed reveals it.

**Two fixes, and both are wanted:**

1. `SteamGameImport.Plan` should prefer the manifest's per-game `modPath` over the engine preset,
   falling back to the preset only when the feed has nothing.
2. `RegistrationRefresh` needs a `ModPath` member with the same untouched-preset-default rule the
   others use — the feed's value wins when the stored value is still the preset default and the user
   has not set one. That repairs the 54 already-registered cases without overriding a deliberate
   choice.

**User-facing remedy today:** the empty-list banner offers **Check setup**, which is A2's repair
surface — point the mod folder at the right path and the game works. That the repair UI exists is why
this is `M` and not urgent, but a user who does not know the correct RE-Engine path cannot use it.

**Related but distinct from A1**, which fixed refresh for extensions and grouping. This is the same
lesson arriving at the field where it costs the most.

### A17. REFramework is invisible to the launcher · `M` · Este, 2026-08-18

Falls out of A16. REFramework is the loader for every RE Engine game — Monster Hunter Wilds and
World, Resident Evil 2/3/4/7/8, Dragon's Dogma 2, Devil May Cry 5 — and 626 has no `KnownFramework`
entry for it. Consequences, all seen live on Wilds:

- **It is not listed as a framework.** The six lua mods showed as mods; the thing that loads them did
  not appear at all, so a user reading the launcher cannot tell REFramework is installed.
- **It carries no version.** The overlay in-game reads `v1.5.9.1`; the launcher knows nothing.
- **Nothing noticed it was seventeen months stale.** `dinput8.dll` dated 2025-03-10 against a game
  binary dated 2026-08-17. That mismatch is the crash, and it is trivially detectable — compare the
  proxy's timestamp against the game executable's.
- **`reframework/plugins/*.dll` load regardless of `autorun`.** Disabling every mod in `autorun`
  leaves plugins injecting. 626 manages neither.
- **`_CatLib`, `mhwilds_overlay`, `utility` were left in `autorun`** because the scan classified the
  six `.lua` FILES as mods and not the folders beside them. Those are libraries the mods depend on,
  so leaving them is arguably right — but it is accidental rather than decided.

**A stale-loader warning is the valuable half.** "Your loader predates this game build" would have
caught this before launch, and the same check applies to every framework the launcher knows: compare
the loader's file date against the game executable's, warn when the game is newer. Cheap, and it
addresses the single most common way a modded game breaks after a patch — which is exactly what a
launcher exists to notice.

Catalog work per the documented path: `KnownFramework.cs` + a test + `NOTICE` attribution
(REFramework is by praydog). Note it installs as a bare `dinput8.dll` at the game root plus a
`reframework/` tree, so `KnownFramework.Classify` needs both to avoid matching every other proxy that
ships under that filename — the same "several loaders share this name" honesty the adoption dialog
already shows.

**A catalog entry alone will not be enough, and this is the part worth designing.** Este asked
whether toggling would reach the loader once vanilla launch could. It will not, and the reason is a
split in the framework subsystem:

| | Knows about | Drives |
|---|---|---|
| `FrameworkDeps` | any framework PRESENT on disk, by relative path | the `NEEDS X` / `MAY NEED X` chips |
| `FrameworkRegistry` | only what **626 itself installed** (`<dataDir>/frameworks/*/install.json`) | listing, disable/enable, uninstall |

**A framework the user installed by hand lands in the gap: detected well enough to raise a chip,
invisible to every surface that could manage it.** That is Este's REFramework exactly, and it is why
`ActiveFrameworks` returned empty for Wilds — the vanilla path was rescued by the *proxy* lane in
A16, not the framework lane, which would also have found nothing.

So A17 is really "let the launcher manage a framework it did not install": detect presence via the
`FrameworkDeps` paths, offer to adopt it into `FrameworkRegistry` (metadata only, files not moved —
the same shape mod adoption already uses), and only then do toggle, version and the stale-loader
warning become possible.

Related to **A13**, which found the same subsystem OR-ing a runtime and its loader as though they
were alternatives. Two findings, one week, one corner of the code — worth reviewing `FrameworkDeps`
and `FrameworkRegistry` together rather than patching each symptom.

### A18. The MCP never applies the remote feed, so it and the app disagree · `S` · found 2026-08-18

`RemoteManifestCache.ApplyCached` is called from exactly one place: `RemoteManifestSource`, an **App**
service. `src/ModManager.Mcp/Program.cs` builds the host, registers the tools and runs — it never
applies the cached feed. So the MCP reads the **embedded snapshot only**.

Proven on Monster Hunter Wilds the same morning A15 shipped:

| | says the mod folder is |
|---|---|
| the app (feed applied at startup) | `reframework/autorun` |
| `get_game_shape` over MCP | `mods` |

**Every manifest correction is invisible to the agent.** A1 taught registrations to pick up corrected
extensions and grouping; A15 added the mod path. None of it reaches a tool reading through the MCP,
which reports the frozen registration as though it were the truth.

This is the parity rule from section E broken structurally rather than by omission — not "the agent
sees less detail" but "the agent sees a different answer". It is also how a probe run against bare
Core reported `exts=pak, modPath=mods` for a game the app resolves as `lua,pak,dll` +
`reframework/autorun`, and would have produced a confidently wrong conclusion if it had not been
checked against the app.

Fix is small: apply the cached feed at MCP startup the way `Program.Main` does for the app. Worth a
test that pins app and MCP to the same resolved answer for one game, because the two entry points
drifting again is the actual risk.

### A19. Intake flattens a nested mod tree · `M` · found 2026-08-18

Asked whether 626 could take over the thirteen mod archives Fluffy holds for Wilds. `Scanner.PlanIntake`
(plan-only, no writes) says it would try — and would break them.

```
zip!_CatLib/reframework/autorun/_CatLib/action.lua   ->  RelPath = action.lua
zip!reframework/autorun/utility/Statics.lua          ->  RelPath = Statics.lua
zip!.../autorun/mhwilds_overlay/backups/draw.lua     ->  RelPath = draw.lua
```

`RelPath` is the bare filename, and `ExecuteIntake` copies to
`Path.Combine(primary.Abs, item.RelPath)` — so every file lands flat in the mod folder regardless of
where it sat in the archive. For a pak game that is correct and has always been correct: a pak is one
file and its name is its identity.

**For a script-tree mod it is destructive.** CatLib is a library of ~20 lua files under
`autorun/_CatLib/`; flattened, none of them resolve. Two different mods each shipping
`autorun/utility/Statics.lua` collide on one name, and the second silently wins. The install would
report success and not work — the failure mode this repo keeps finding and keeps disliking.

Wanted: preserve the path **below the mod-location root**. The archives already state it — every one
of these carries a full `reframework/autorun/...` path inside, so the rule is "find the segment that
matches the declared mod location and keep everything under it", not "guess". Note that some zips
wrap it in a release folder (`KittyBig-v1.0/reframework/autorun/...`, `_CatLib/reframework/...`), so
the match cannot assume the path starts at the root.

Blocks taking over any script-tree game: RE Engine, and the same shape applies to UE4SS Lua mods and
BepInEx plugin folders.

### A20. Create the declared mod folder when the feed named it · `XS` · Este, 2026-08-18

Este: *"When taking over a game, we're free to make the correct folder if we know what the correct
folder is named."*

Today a registration whose mod folder does not exist yields the empty-list banner — *"No mods found
here, and the folder this game is set to look in doesn't exist"* — and stops. That is the right
caution when the path is a guess. It is unnecessary when the feed states the path: creating an empty
directory is not a destructive act, it is what installing the first mod would do anyway, and the
alternative is telling the user their setup is broken when it merely has not started.

Scope it to the case where the path came from the manifest rather than from a preset default or a
hand-typed value — those are exactly the cases where a missing folder might mean the path is wrong,
and inventing it would paper over the real problem A15 was about.

### A21. `GameShape` counts files that are not on disk · `S` · found by the MCP, 2026-08-18

Asked whether the MCP could smoke the provenance work. It can read but not act (E1) — and on the
first read it returned output that contradicts itself:

```jsonc
"declaredLocations": [{ "path": "mods", "exists": false }],
"contentRoots":      [{ "relativePath": "mods", "fileCount": 6, "insideDeclared": true }],
"alignment": "Aligned",
"notes": [ "Declared mod location 'mods' does not exist on disk",
           "Mods are where the registration says they are." ]
```

Six files reported inside a folder that is not there, and both notes at once. Verified on disk: the
folder genuinely does not exist.

**Cause.** `GameShape` builds `ContentRoots` from the MOD ROWS — each mod's declared location plus its
file paths — and never checks that the directory exists or that the files are in it. All six mods here
are **disabled**, so their files sit in the holding folder while the rows still report the location
they would occupy. `Alignment` then reads `Aligned` because every fabricated root is `InsideDeclared`,
and the two notes are computed independently from different inputs and never reconciled.

**Why it matters more than the contradiction looks.** `get_game_shape` exists to answer one question —
*are this game's mods where the registration says they are* — and it answered **Aligned** for a game
whose mod folder does not exist. A21 is the same shape as everything else found this week: a
confident wrong answer rather than an error. A4 built this type precisely to stop that.

**Not a regression from the provenance work** — `GameShape` is untouched by it. A game with every mod
disabled is enough to trigger it, which is not an exotic state.

**Shape of the fix.** A content root is a claim about disk, so it should be verified against disk:
count files that exist, drop a root whose directory does not, and let `Alignment` fall out of what
survives. A game with mods that are all disabled is honestly `NoMods` on disk — or wants a state that
says so, since "no mods present" and "no mods at all" are different answers and the caller may care.
Reconcile the notes from one computed picture rather than two.

**Credit where due:** the read-only MCP found this by being asked a question the UI never asks. Worth
remembering when weighing E1 — the read half is already earning.

### A6. Follow-ups the registration-repair reviews found and deliberately deferred · `S`

Raised during the per-task and whole-branch review passes on `feat/registration-repair-ui`. None
blocked the merge; each was consciously deferred with a reason.

- `DataDirMove.Plan` walks the whole data directory on every `RegistrationChange.Plan` call, so the
  setup dialog re-walks it on every pause in typing (a 250ms debounce mitigates but does not fix it).
  Fix: memoize `DataDirMove.Plan` in Core, keyed on the normalised `(from, to)` pair —
  `DataDirMove.Norm` already exists for this.
- `RegistrationChange.SameLocations` compares list COUNT first, so any caller that reshapes the
  location list reads as a user edit and false-pins `modLocations`. That was patched twice at the
  call site during this work (the `>1` and `==0` cases). **The obvious test is not the fix.**
  `Plan(stored: [], proposed: [])` already passes against today's implementation, so writing it buys
  a regression guard and nothing more — and it does not touch the actual hazard, which is a CALLER
  reshaping the list (`1 → 3`, `0 → 1`) and the planner reading that reshape as a stated choice. A
  test that proves anything has to assert on a reshape, or A6 gets closed with a green test that
  proves nothing.
- **Two decisions still in the App that belong in Core**, both currently unreachable by a test:
  1. The pin target. `RegistrationRepairService.SaveAsync` writes `Scanner.DataDirForGame(stored)` —
     STORED, not proposed, because the point of a pin is to name where the data already is. Getting
     that one word wrong writes a `dataDir` pointing at a folder with nothing in it. The rule deserves
     to sit next to `DataDirMove` behind a test.
  2. The orphan message. The double-failure and source-survived branches are the highest-stakes
     strings in the feature and are asserted by nobody. `DataDirMove.SpaceRefusal` is already
     `internal` precisely so a test can hold a user-facing string to account; the same precedent
     applies here.
- **A breadcrumb at `move.To`.** The move runs before the registry write, so process death in the
  window between them leaves the data at the new path and the registration at the old, with no record
  that a move happened. A small marker file written into the target before the move and removed after
  the successful write would make that recoverable on next launch instead of silent. (The read-back
  added for the concurrent-write case covers the writer-clobber half; it cannot cover process death.)
- **Only the first mod location is editable.** The setup dialog edits `ModLocations[0]` and carries
  the rest through untouched. A user whose SECOND location is wrong has no repair path here at all —
  they are back to hand-editing `games.json`, which is the situation this feature exists to end.
- **`games.json` has no lock.** `LoadRegistry → change → SaveRegistry` is shared, unlocked, and
  app-wide (`StampLaunch`, `SetActiveGame`, `Redetect`, discovery, and now the repair save). The
  repair save reads its write back and reports a clobber rather than going silent, which is the cheap
  half. Real serialisation — a single writer, or a file lock — is the proper fix.

### A3. `.archive.xl` sidecars are invisible and get orphaned on toggle · `S` · **DONE 2026-08-05** (`45edf7e`)

Cyberpunk's ArchiveXL mods ship `Foo.archive` **plus** `Foo.archive.xl` (91 of them in Este's
install). `DiscoverySweep.Extension` and `ctx.FileRe` both read only the final segment, so `.xl`
never matches `archive` and the sidecar is not part of the mod. Toggling the mod off moves the
`.archive` to holding and **leaves the `.xl` behind**, pointing at a file that is no longer there.

**Fix shape.** Treat a compound sidecar as belonging to its base mod key so it moves with it.
Needs care: the sidecar must not become its own row, and the grouping has to survive
`Scanner.ModKeyFor`. Verify what ArchiveXL actually does with an orphaned `.xl` before sizing.

### A22. Installing a mod never asks the ban-risk question · `S` · found 2026-08-18

**The enable gate has seven call sites. Intake is not one of them.** Found while scoping A14 part 3,
which wanted to offer "install these" from the adoption dialog and would have inherited this.

Every path that turns a mod ON consults `GateBanRiskEnableAsync` — the row toggle, enable-all, the
loadout apply, the variant chips. On a high-risk, un-acked game it warns and waits for an explicit
acknowledgment, which is the rule: *detection is fine; the enable path must warn and require an
explicit ack, and never enable without it.*

`AddModsAsync` — the drop / file-picker / tools-panel intake — calls none of it. And
`Scanner.ExecuteIntake` copies into `primary.Abs`, the game's live mod folder. So a dropped zip is
installed **enabled**, on any game, with no warning and no ack.

| | |
|---|---|
| Gate call sites | 7 — all toggle / enable-all / loadout / variant paths |
| `AddModsAsync` callers | 4 — drop handler, file picker, tools panel, internal — none gated |
| Where intake writes | `primary.Abs` (the live mod folder), via `CopyPlanned` |

**Why it matters more than it looks.** Drag-and-drop is the app's flagship gesture and the one a new
user reaches for first. The toggle gate protects a user who turns an existing mod on; this is the
user who has no mods yet, on a game the app itself flags as ban-risk, taking the most obvious action
available. The ack is exactly as warranted there, arguably more.

**Not a regression, and not urgent-dangerous by itself** — the ack persists per game, so a user who
has acked once is unaffected either way, and the mods installed are as reversible as any other.
What it breaks is the promise that nothing goes live on a ban-risk title without the user saying so.

**Shape of the fix.** One gate call at the top of `AddModsAsync`, before any file is placed —
refusing there leaves the game folder untouched, which is the validate-then-extract order anyway.
Worth deciding: one ack per drop batch (recommended — a ten-file drop is one decision), and whether
a framework or tool install counts (probably yes; a loader going live is the thing anti-cheat sees).

**Blocks A14 part 3**, which would otherwise put an Install button on a dialog and inherit the gap
behind a second door.

### A23. A loose-root game lists its own folders as mods · `M` · found walking the smoke list, 2026-08-18

Death Stranding 2 lists **15 mods**. Nine of them are mods. The other six are the game.

| Row | What it actually is |
|---|---|
| `LocalCacheWinGame` | Decima's local cache folder |
| `steaminput` | Steam input configs |
| `tools` | a game folder |
| `uds` | a game folder |
| `reshade-shaders` | ReShade's shader folder — **already listed as a file of the ReShade row** |
| `_MODS_STAGING` | Este's own staging folder |

**The nine real ones are classified correctly**, which is worth saying: ReShade as `graphics`, three
`plugin` rows (DeathStranding2Fix, DollmanMute, Zipliner), three `shaders` rows (DeathStranding2UI,
renodx, ShaderToggler), and `version.dll` / `dxgi.dll` as `loader`. The loose-root lane is doing its
job. The half that misfires is the folder lane running beside it.

**Cause.** A loose-root registration declares its mod location as the game root itself — DS2's reads
`mods` → path `.`, which is correct and is how loose-root works. The folder-scan lane then treats
every top-level DIRECTORY under that location as a folder mod. In a game whose mod folder is a
dedicated `~mods` or `Mods`, that is right. In a game whose mod folder IS the game root, every
directory the game ships becomes a row.

**Severity: contained, by something built for a different reason.** All six are `class: library` and
therefore `ReadOnly = true` — the Wave 1 rule that *"nothing readable declares that it needs it" is
not the same as "nothing needs it"*, so a library nothing can be shown to require stays switched on.
That rule was written for `_CatLib` on Monster Hunter Wilds and it is what stops the launcher here
from offering to disable `steaminput`. **The launcher does not let you break the game.** It is still
wrong in three ways:

1. **The count lies.** The status line reads `15 of 15 enabled` on an install with nine mods, and
   `get_game_shape` reports `modCount: 15, rowCount: 15`. Both halves of A12's honesty work report
   the same wrong number confidently.
2. **The copy is false about specific folders.** Every one of the six says *"A shared library other
   mods load."* `steaminput` and `LocalCacheWinGame` are not libraries and nothing loads them.
3. **`reshade-shaders` is double-counted** — once inside the ReShade row's file list, once as its own
   row.

**Play vanilla was the path where containment might not have held. RUN on the rig 2026-08-18: it
holds, and the run corrected what I first wrote here.** `LocalCacheWinGame`, `steaminput`, `tools`,
`uds` and `_MODS_STAGING` all stayed in the game folder while the nine real mods stepped aside.

**But `reshade-shaders` moved — correctly, and that is the instructive part.** It went to
`loose-disabled/reshade/reshade-shaders`, because the ReShade row CLAIMS that directory as one of its
files. So the guard here is not `ReadOnly` alone, which is what I claimed before running it. It is
`ReadOnly` **plus nothing else claiming the directory**. The read-only library row for
`reshade-shaders` was excluded from the step-aside exactly as designed, and the folder moved anyway,
under a different row that owns it.

That is the double-count above showing its real consequence rather than a cosmetic one: a directory
belonging to two rows is protected only as strongly as its *weakest* claim. Here that is correct —
`reshade-shaders` is ReShade's. It would not be correct if a mod ever claimed a directory the game
owns, and nothing in the current design would stop it.

**Shape of the fix.** When a declared mod location resolves to the game root AND the mechanism is
`LooseRoot`, the folder lane should not auto-list top-level directories. A folder becomes a row
there only on evidence — a catalog hit, an install record (the A19/provenance work already tracks
what 626 placed), or a loose-root mod claiming it, the way ReShade already claims `reshade-shaders`.
Everything else in that root is the game until something says otherwise.

**Related:** the inverse of A21, which counted files that were not on disk. This counts directories
that are on disk and are not mods. Both come from a lane inferring membership from position alone.

### A24. The loader warning names a consequence it cannot know · `S` · found walking the smoke list, 2026-08-18

Toggling `version.dll` off on Death Stranding 2 raises:

> **This mod is a loader**
> *"Version (ASI Loader)" is the loader other mods inject through — disabling it disables every ASI
> plugin.*

**The dialog is right to appear and wrong about what it says.** Read the file:

| File | What 626 calls it | What it actually is |
|---|---|---|
| `version.dll` (28.8 MB) | "Version (ASI Loader)" | **DLSS Enabler 4.5.2.2** — *"DLSS enabler for DX12 GPUs"* |
| `dxgi.dll` (5.5 MB) | "Dxgi (ASI Loader)" | **ReShade 6.7.3.2148**, by crosire |

Neither is an ASI loader. Disabling `version.dll` costs the user DLSS and frame generation — a
rendering change they would notice immediately — and has nothing to do with ASI plugins. The three
`.addon64` mods on that install (DeathStranding2UI, renodx, ShaderToggler) are **ReShade addons**,
so they ride on `dxgi.dll`, which the dialog would describe with the same sentence.

**Cause.** The row name and the warning are derived from the FILENAME. `version.dll`, `dxgi.dll`,
`dinput8.dll` and friends are known proxy names, so anything wearing one is labelled an ASI loader
and the consequence sentence follows from the label rather than from the file.

**The app already knows better somewhere else.** The discovery review dialog says, of exactly this
situation: *"This is the loader other mods ride on, not a mod itself. Several different loaders ship
under this filename, so it can't be named from the file alone."* That copy is correct and careful.
The mod-row warning makes the opposite move — it names the loader AND asserts what disabling it
costs, from the same evidence that made the other surface refuse to guess.

**And the evidence is right there.** `FileVersionInfo` on both DLLs returns the product name, the
description, the company and the version, which is how the table above was built. No catalog entry,
no fingerprint, no network call — one Win32 call the launcher does not make.

**Shape of the fix**, cheapest first:

1. **Read the version info** and use it when it is there: *"version.dll — DLSS Enabler 4.5.2.2"*
   beats *"Version (ASI Loader)"* on every axis, and it costs one call.
2. **Stop asserting the consequence when we do not know it.** Where the file names itself, say what
   it is. Where it does not, fall back to the discovery dialog's honest line rather than to a
   confident sentence about ASI plugins.
3. **Only then consider a catalog.** Known proxies could carry a real consequence string — ReShade:
   *"you will lose ReShade and its addons"* — but that is a nicety on top of not being wrong.

**What passed, and is worth keeping.** The warning fires at all rather than moving a loader
silently; Cancel is the default button and the destructive option is secondary; and cancelling
wrote nothing — `version.dll` still on disk, hash unchanged. The mechanism is sound. Only the
sentence is wrong.

**Related:** A13, where a framework claim was assembled from filenames without checking what the
files were. Same root: a name is not an identity.

### A25. Loose-root and taken-over intakes record nothing they placed · `S` · found walking the smoke list, 2026-08-18

Dropped `SmokeDrop626.zip` on Death Stranding 2. The mod installed correctly — extracted loose into
the game root, no spurious subfolder, a row appeared classed `PLUGIN`, no ban-risk friction on a
game that has none. Everything about the install was right except that **626 did not write down
that it had done it**.

No `installs/` directory exists under that game's data dir. The row reads *"Detected: loose .asi in
game root"* — the inference — about a file the launcher had placed there thirty seconds earlier.

**Cause, one early return.** `Scanner.ExecuteIntake` writes the install manifest at the very end,
deliberately: *"written LAST, after every copy has either succeeded or been skipped, so a manifest
never claims a file that is not on disk."* That reasoning is right. But two branches return before
reaching it:

| Branch | Line | Recorded? |
|---|---|---|
| Folder intake (`~mods`, `Mods`, paks) | falls through to the end | yes |
| `form == "loose-root"` → `DirectInject.Execute` | early return | **no** |
| Folder owned by another manager (`ToolOwnership.Owned`) | early return | **no** |

`DirectInject.Execute` writes no manifest of its own — `ModInstallRegistry.Save` has exactly one
call site, and it is past both returns.

**What it costs.** The provenance design's whole premise is *track what we placed, infer the rest*.
On every loose-root and direct-inject game, nothing is ever tracked, so the second half carries the
first: every row is inferred, including the ones 626 installed itself. Concretely, `ModListing`
builds `claimed` from `ModInstallRegistry.List(ctx.DataDir)` and gets an empty set for these games,
so a mod the launcher installed cannot say which files are its own — which is the question uninstall
needs answered.

**Why it did not show up before.** Inference is good enough to make the row look right, and the row
looking right is what anyone would check. It took dropping a mod with a name nothing could have
guessed, then looking for the record rather than the row.

**Shape of the fix.** Record on the way out of each branch rather than at one shared end. Either
have `DirectInject.Execute` return the placed-file list and write the manifest at both call sites,
or hoist the write into a small helper both branches call. The last-write-wins ordering must
survive — a manifest that claims a file which was skipped is worse than no manifest, because an
uninstall would act on the claim.

**This is a prerequisite, not bookkeeping — it is what blocks uninstall on these games.** A loose-root
row never shows the trash can: `canUninstall: !directInject && !looseRoot && !rep.ReadOnly`, with the
reason stated on the field — *"Direct-inject mods can be toggled (reversible move) but not uninstalled
here — we never delete loose files in the game's exe folder."* That refusal is right. In a folder
holding the game's own DLLs, deleting is too dangerous when you cannot prove which files are the
mod's.

**Provenance is exactly that proof.** For a mod 626 placed itself, the manifest is an exact file list,
which is the difference between deleting what we put there and guessing. Fixing this would let
uninstall be offered on loose-root games for tracked mods ONLY, while a hand-installed mod keeps the
toggle and no delete — the honest split, and one the launcher currently cannot make because it
records nothing.

**Games affected:** every `loose-root` registration (Death Stranding 2 and any Decima game), plus
anything installed into a folder another manager owns.

### A26. Uninstall deletes the files and keeps the record · `XS` · found walking the smoke list, 2026-08-18

Uninstalled a test mod on Monster Hunter Wilds. It took exactly the one file its manifest claimed,
left the other ten probe files alone, and left the real mods alone — the destructive path is
precise. Then the install manifest stayed on disk, claiming a file that no longer exists.

**`ModInstallRegistry.Remove(gameDataDir, installId)` exists and has zero call sites.** Written,
never wired.

**The invariant is held at write time and dropped at delete time.** The write site says why it
matters, in its own words: *"a manifest never claims a file that is not on disk — the claim is what
an uninstall would act on, and a claim on a missing file is how a cleanup deletes the wrong thing
later."* Careful about never writing such a claim; nothing removes one.

**What a stale claim costs.** `ModListing` builds its `claimed` set from
`ModInstallRegistry.List(...)` to decide what is tracked rather than inferred. A claim on a deleted
path means a file that later reappears at the same name is attributed to an install that is gone.
Re-dropping the SAME source self-heals — `IdFor` keys on the source archive, so `Save` overwrites —
but a different source landing on the same path leaves two live claims on it.

**Fix.** `UninstallMod` removes the manifests that claim the files it deleted, in the same call.
Order matters the same way it does on the write: delete first, forget second, so a failed delete
never leaves the launcher believing it owns nothing while the files are still there.

**The fix does not clean records orphaned before it, and that is fine.** One survived on Wilds — the
install record for a mod uninstalled minutes before the fix landed, still claiming
`smoke_probe_626.lua` after the file was gone. Removed by hand.

**Do NOT add a prune-claims-whose-files-are-missing self-heal.** It reads as the obvious tidy-up and
it is wrong: **disabling a mod MOVES its files to the holding folder**, so a live mod's claim points
at a path that is legitimately absent from the mod folder for as long as it is switched off. A prune
on that rule would forget the provenance of every disabled mod, which is precisely the information
needed to restore or uninstall it. If a self-heal is ever wanted, it has to consult the holding folder
too — and at that point it is not a tidy-up any more.

**Found only because A25 shipped hours earlier.** Before that, loose-root and direct-inject installs
wrote no manifests at all, so there was nothing to leave behind. Fixing one gap is what made the
next one visible — and it is the same lesson as A25 itself: check the record, not the row.

### A27. A10 left the downgrades it was filed about · `S` · found walking the smoke list, 2026-08-18

The Updates view on the live install, after A10 shipped:

| Row | Renders |
|---|---|
| Fast Craft | `1.1 → 1` |
| Elden Ring Ultrawide Fix | `1.0.1 → 1.0.0` |

**Those are the two examples A10 was filed about.** Its own entry names them: *"plus `1.1 → 1` and
`1.0.1 → 1.0.0` — two apparent DOWNGRADES"*. A10 fixed the `unknown → 1.2.1` case and the
normalise-equal case and left these exactly as they were.

**The plan contradicted itself and I shipped the weaker half.** Both of these are in
`2026-08-18-wave2-updates-that-tell-the-truth.md`:

> pending because versions differ, both known → `1.2.0 → 1.3.1`, **unchanged**

> **Not attempting to order arbitrary version strings.** … Where we cannot say which is newer, we do
> not draw an arrow.

The second is the principle; the first is what got built. An arrow IS a claim about direction, and
the code draws one whenever two known versions differ — including when it points backwards.

**Refusing to order was right; refusing and then drawing the arrow anyway was not.** The fix is not
a general comparator. It is: draw the arrow only where the ordering is provable under a strict
numeric parse, and where it is not, name both sides without implying direction.

- `1.2.0` → `1.3.1` — both parse, ascending → keep the arrow
- `1.1` → `1` and `1.0.1` → `1.0.0` — both parse, DESCENDING → no arrow
- `1.0.0.1` → `1.0.0.1-hotfix` — does not parse → no arrow, because "is hotfix newer" is exactly the
  guess we declined to make

That last case is a real cost: it is probably newer, and the neutral wording says less than an arrow
would. Saying less is the point — the row still names both versions, and the user can decide.

**A question this raises and does NOT answer: should a provably-AHEAD mod be listed at all?**
`ModUpdateSummary` marks a mod pending when `latest != m.Version` — any difference, in either
direction. Fast Craft records 1.1 installed against a Nexus latest of 1, and the Ultrawide Fix records
1.0.1 against 1.0.0. Neither is behind anything.

Now that ordering is available where it is provable, the pending RULE could use it and drop those two
rows entirely. **Deliberately not done here.** A10's own reasoning applies: the recorded installed
version is often derived from a filename and may simply be wrong, and dropping the row would hide a
real update to make a display problem go away. Changing what is LISTED is a different decision from
changing what a row SAYS, and it wants its own entry and its own evidence — starting with why those
two versions disagree with Nexus in the first place.

**Why it survived the wave.** The tests asserted the reason a row was listed and the equivalence
rule, and never asserted what a descending pair renders as. The entry named two examples and
neither became a test case. Live data had both, on two different games, the whole time.

### A28. The Updates rows are unaddressable, and the harness calls that a pass · `S` · Este asked why, 2026-08-18

Este, after a verification he had to perform by hand: *"why couldn't we test this with the MCP and
with the screenshotting tools that we added in for this?"* Two answers, and only one of them is my
habit.

**The screenshot channel works.** Attached to the running app, captured the Updates view, read the
PNG, confirmed all four rows and that no arrow points backwards. No human needed. That half was
simply not used.

**The UIA channel is blind on this view.** `Find-AllByIdPrefix 'UpdateRow.'` returns **0** while the
view is open and its text is plainly readable. `UpdateRow.RowAutomationId` exists in the code-behind —
`$"UpdateRow.{Pending.ModKey}"` — and `UpdatesView.xaml` **never binds it**. The only
`AutomationProperties.AutomationId` on that entire view is `UpdatesBackButton`.

This is the templated-control half of `.claude/rules/automation-ids.md` skipped: the rule says a row
template binds its id from a stable key, the property was written for exactly that, and the binding
was never added.

**And the harness scores it green.** Its `updates-view` case reads
`$rows = @(Find-AllByIdPrefix $t 'UpdateRow.')` and reports `"$($rows.Count) update rows"` without
asserting anything about it. It has been printing **0 update rows** and passing. Same false-green
shape as the game-view cases fixed hours earlier in E3 — those were fixed because they were tripped
over, and this one was not.

**What it cost.** A27 — arrows pointing backwards on two real rows — was found by a person looking at
a screen. The tooling could have caught it: read each row's version text through UIA and diff it
against what `ModUpdateSummary` computes. That check is impossible while the rows have no ids, and
the harness reported healthy throughout.

**Fix.** Bind the id in the row template, and make the case assert that the row count matches the
group count rather than printing it. A case that reports a number nobody checks is a case that cannot
fail.

## B. Features

### D1. Settings has no information architecture · `M` · Este, 2026-08-16

The vibe-glow campaign gave Settings the same treatment as the other sixteen dialogs — square shell,
3px accent rail, stencil eyebrow, the type ramp. That is chrome. Nobody ever filed the thing Este
actually notices when he opens it: **the dialog has no organising principle.** Identity, themes,
Nexus, tools, restore points, plugin management and Safe Clear share one scrolling column, in the
order they happened to be built, with no grouping that reflects how often a setting is touched or
what it belongs to.

Not a glow finding and not a regression — the audit scored conformance and consistency, and Settings
passes both. A surface can be perfectly conformant and still be hard to find things in.

Wants a real design pass, not a reshuffle: what are the actual groups, what belongs on a first
screen versus behind a disclosure, and which of the current entries are settings at all versus
actions that happen to live there (Safe Clear, restore points, "install / refresh Nexus plugin").
Own spec → plan → build. The dialog is `src/ModManager.App/SettingsDialog.xaml` and it is long.

**Re-raised by Este 2026-08-17**, alongside the agent-access want in section E. Worth doing in that
order rather than in parallel: E2 gives a way to actually see the dialog's current state described
back, and a redesign that nothing can verify is how the four bugs in 0.18 got out.

### D3. The avatar picker opens on an empty box instead of the app's own icon · `XS` · Este, 2026-08-17

Settings → Identity offers "Pick an image — a Discord avatar, a screenshot, a photo. Use it as the
launcher's icon and derive a theme from its colors." Next to that copy is an **empty grey square**
until the user picks something. See `docs/store-assets/screenshots-0.17/06-settings.png` — it is
blank in the shipped listing shot, which is how long it has looked like that.

**Seed it with the app's own icon.** The control is asking the user to replace something, and it
currently shows nothing to replace. An empty box makes the user guess what the field does and what
size or shape is wanted; the 626 mark sitting there answers both without a word of copy, and it
demonstrates the outcome — *this is the thing that will change* — rather than describing it.

It also makes the derived-theme checkbox underneath legible. "Save a theme derived from these colors"
is abstract next to a blank square and obvious next to an actual image.

Small print worth getting right, because the difference is the whole point:

- The seeded icon is a **placeholder, not a selection.** Opening Settings and pressing Apply must not
  write the app icon as if the user had chosen it, and "Remove avatar" should return to the seeded
  state rather than to blank.
- Use the same asset the app already ships as its window/tile icon, so it is recognisably *this app*
  and not a generic glyph.

Overlaps **D1** — the Identity block is one of the sections that design pass has to place. Cheap
enough to do on its own first; just do not let D1 re-litigate it afterwards.

### D2. Decide the default theme deliberately, and keep the Store honest about it · `S` · Este, 2026-08-16

**Done for the code half (2026-08-16):** the first-run default is back to `626-labs` (navy),
reverting `260e831` which made Forge the default as part of the vibe-glow reveal. Forge is unchanged
and still one click away. One line, `ThemeService.cs:25`.

The reasoning worth keeping: a flagship look is a strong opinion to impose on someone who has not
asked for one, and the default is the one theme choice made FOR the user rather than BY them.

**Answered 2026-08-16 (Este): 0.17.0.0 IS live and is the newest published version.** So the live
listing carries the seven Forge screenshots in `docs/store-assets/screenshots-0.17/`, and 0.18.1
opens navy. `docs/store-listing-0.17.0.md` is superseded by `docs/store-listing-0.18.1.md`.

**Still open — retake the seven screenshots in navy.** Not a certification problem: Forge is a theme
the build genuinely ships and can be set to in one click, so nothing in the listing is false. It is a
first-impression mismatch, amber on the Store page and navy on launch. 0.18.1 was submitted with the
Forge set deliberately, because the fixes in it address users who could not tell whether adding a
game had worked, and that should not wait on photography. The reviewer letter discloses the mismatch
rather than hiding it.

When retaking:

- **On the main box, not the clean test PC.** The 0.17 set shows six games with real cover art; the
  test box is nearly empty and would make a worse listing than a differently-coloured one.
- **All seven or none.** A half-swapped set reads as a rendering bug.
- Ship as a **listing-only submission** — no new package needed, and those certify quickly.

Related want, not yet decided: a first-run theme picker would remove the question entirely by making
the user choose. That ties to the onboarding task already on the 626Labs dashboard.

### B1. Close to tray · `S` · Este, 2026-08-05

Closing the window exits the app. Offer **close-to-tray** as an option, which also buys a tray
surface worth having: quick per-game actions, launch targets, update-available state, and a real
Quit. Settings toggle, default off (closing should still mean closing until the user says
otherwise).

### B2. Discovery falls back to a live Nexus name search · `M`

Tier 2 matches against the per-game `ModNameIndex`, seeded from the top ~500 browsable mods. That
is already "the browsable Nexus catalog, not just what we installed" — but a 194-mod Cyberpunk
library is mostly outside any top-500. Unmatched candidates should fall back to a **live text
search**, the way `LooseIdentify.ProposeAsync` already does (self-timeouting search delegate,
budget-capped). Reuses machinery that exists; the cap and the daily-budget interaction are the
design work.

### B3. Adopt another manager's library · `L`

The old Nexus Mods App keeps a 214 GB store at `%LOCALAPPDATA%\NexusMods.App\DataModel` holding
mods that are **downloaded but not deployed**. Anything deployed is already in the game folder and
therefore already ours to see; the undeployed remainder is invisible and always will be to a
game-folder sweep. If we ever adopt from a foreign library, adopt them **disabled** — an undeployed
mod is off, and turning it on is the user's call, through the normal reversible path.

**Prerequisite.** md5-ing a 214 GB store is not a sweep. This needs the foreign manager's own index
read, not brute force.

### B4. One mod, many trees · `M`

A single Cyberpunk mod spans `archive/pc/mod` (the archive), `r6/scripts/<Mod>` (redscript),
`r6/config` (tweaks), `red4ext/plugins`, and `bin/x64/plugins`. Este's install has all of these.
A game's single `modPath` can only ever describe one, so the launcher sees at most a third of such
a mod and would toggle only that third — a real reversibility concern, not just a display gap.

Needs a manifest schema conversation (multiple named mod trees per game, and which are toggle-safe)
plus a Core listing change. Spec B territory.

## C. Follow-ups from the identify-consolidation branch

Raised by the per-task and final whole-branch reviews, adjudicated as ship-now/fix-later. Listed
worst-first. None blocked the merge; all were consciously deferred with a reason.

### C1. One long-operation slot, guarded per-method · `S` · **DONE 2026-08-06**

`IdentifyMyModsAsync` now has a re-entrancy guard. `EnrichMetadataAsync` does not, and both claim the
same `_longOpCts` / `IsCancellable` / `IsBusy` slot. Because Advanced items are deliberately always
visible and never disabled (*guard, don't hide*), a user can start "Refresh details from Nexus" while
an identify run is in flight: Stop then cancels the wrong run, whichever finishes first clears the
busy state for both, and two writers reach `Scanner.WriteManyMeta` while a third reads `_allRows`.

Not a data-loss or reversibility break — both writers write correct data through the same per-key
path — but a lost Stop button and a stale ring. `master` already carried this hazard with *two*
unguarded owners, so the branch strictly reduces exposure rather than widening it.

**Fix:** promote `_identifyRunning` to a single `_longOpRunning` guarding every entry point that
claims the slot. Touches a second feature's control flow, so it earns its own test and smoke pass.

### C2. The re-entrancy flag is set outside its `try` · `S` · **DONE 2026-08-06**

`_identifyRunning = true` runs before the `try` block, with five statements between — including two
`[ObservableProperty]` setters that raise `PropertyChanged` synchronously into x:Bind handlers. A
throw in that window latches the flag for the session and silently bricks the app's primary new
action until restart. Low probability, one-line fix: make it the first statement inside the `try`.

### C3. Pass 4 never name-searches the swept candidates · `M` · **DONE 2026-08-09**

The spec says the name search covers *"both swept candidates and existing unidentified rows."* The
delivered run searches `LooseIdentify.Candidates(_allRows…)` only. On a fresh game with a cold name
index, mods found by the sweep but not yet in the list are proposed as "not identified", and naming
them takes a **second** run of the whole action. Working as built, short of the spec as written.

**Fixed, not documented.** Swept candidates now get the same name search the existing rows get, on
the same ladder, behind the same review gate. New `AdoptionEvidence.NameSearch` tier with
`AdoptionProposal.FromSearch`; `WorthSearching` picks the candidates worth spending a call on —
unidentified, and never a proxy loader (it cannot be named from its filename, and attaching an
unrelated identity to the thing every other mod rides on is the worst place to guess).

A miss leaves the proposal exactly as it was — still listed, still adoptable, because visible and
unnamed beats invisible. A throttle stops asking rather than marking the remainder as misses.

### C4. Smaller nits · **SWEPT 2026-08-09** (remaining items marked below)

- ~~`CancelLongOperation` writes `"Stopping…"` before checking there is anything to stop.~~ **Not
  true** — it returns on a null source before writing anything. Entry was wrong when filed.
- Two comments over-claim: one says the two review dialogs' wording "can't drift" while their md5
  detail lines already differ ("Exact match by file hash." vs "Matched exactly by file hash."); one
  says the catalog surfaces gate on connection plus domain when two of the four gate on connection
  plus plugin capability only.
- ~~`IdentifyRunOutcome` carries nine `<param>` tags but has no primary constructor.~~ **DONE** —
  each tag moved onto the property it describes, so the prose reaches IntelliSense.
- ~~The app carries three different remedy strings for "Nexus not connected".~~ **DONE, and it was
  a bug rather than a nit** — four of the ten sites said "(toolbar -> Nexus)" and the only place a
  user can connect is the Settings dialog's "Connect Nexus account" button, so that remedy named a
  place with nothing to click. All ten now use one `NexusNotConnectedMessage` constant.
- ~~`DesignLawTests`' dead-XAML-string allowlist is keyed by element name globally.~~ **DONE** —
  keyed by (file, element), with a test proving an identical name in an unlisted file is still
  reported.
- ~~`TreatWarningsAsErrors` is set project-wide but 36 warnings survive — something is suppressing
  escalation.~~ **Nothing mysterious:** `ModManager.App.csproj:16` sets it to `false` deliberately,
  overriding `Directory.Build.props:7`. All 36 are `MVVMTK0045` — the toolkit asking for partial
  properties instead of fields, for AOT. Re-enabling means migrating every `[ObservableProperty]`
  field in the App, which is a modernisation task, not a nit. Filed as **C7** rather than pretended
  to be a mystery.

### C7. `[ObservableProperty]` fields block AOT-correct codegen · `M`

36 `MVVMTK0045` warnings across the App: the MVVM toolkit wants partial *properties* rather than
fields so CsWinRT can emit correct WinRT marshalling. `TreatWarningsAsErrors` is switched off for the
App project because of them, which means the whole project currently ships without that guarantee —
the cost is not the warnings, it is the guard they disable. Migration is mechanical but broad.

### C5. The same mod can appear twice in the review dialog · `S` · product call

When a mod is matched by BOTH the downloads-archive md5 pass and the name search, the dialog shows
it as two pre-checked rows — one per section — and the button reads "Apply 2 changes" when one will
land. The write-time behavior is correct (the md5 write wins; the name guess is filtered by
`LooseIdentify.ExcludeKeys`) and the summary explains the skip, but the dialog over-promises.

Deduplicating at propose time is NOT the fix — the classifier deliberately does not filter archives
against known row keys, because that would cost the md5 tier its shot at an exact identification.
The honest fix is cross-referencing the two lists by best-guess key when building the dialog and
rendering one row that names both pieces of evidence.

### C6. Name matching attaches the WRONG mod on a variant-family filename · `M` · **DONE 2026-08-09**

Confirmed against the Nexus files list for "Apartment Cats - Custom Cats": all six
`#ApartmentCatsCustoms_*.archive` files on disk ship from that ONE mod page. The sibling mods
("Apartment Cats - Dogtown", "- Corpo Plaza", "- The Glen", ...) are separate mods the user does not
have. Measured on the shipped matcher, with the true owner present in the candidate set:

| filename | picks | |
|---|---|---|
| `_Base` | (none) | miss |
| `_Dogtown_Black` | Apartment Cats - **Dogtown** | **wrong mod** |
| `_Corpo_BlackAndWhite` | Apartment Cats - **Corpo Plaza** | **wrong mod** |
| `_Glen_GreyTiger` | Apartment Cats - **The Glen** | **wrong mod** |

Not "fails to match" — attaches a different mod's identity, above threshold, pre-checked in the
review dialog. A wrong metadata attach is the thing this repo most fears, and it is reachable today.

**Two of the three come from the title-coverage fallback** (`3f45b60`): those queries score 0.38 and
0.43 Jaccard and are accepted only by coverage. The third (`_Dogtown_Black`) clears plain Jaccard at
0.60. The query ladder (`53f2eb9`) is what supplies the sibling candidates in the first place — it is
correct about retrieval, but it widened the pool an asymmetric acceptance rule draws from.

**Why every symmetric or set-based rule fails here.** The true owner and the wrong sibling are both
full subsets of the filename's tokens once plurals are folded, and both score identically. The only
signal that separates them is WORD ORDER, which Jaccard and coverage both discard:

    "Apartment Cats Customs Dogtown Black"  contains  "Apartment Cats Custom(s)"  contiguously
                                            does NOT contain  "Apartment Cats Dogtown"  contiguously

**Proposed rule:** a variant file is its parent's title PLUS a suffix. Prefer a candidate whose full
title appears as a contiguous run in the query, and refuse when two candidates are otherwise
indistinguishable. Verified by hand against the five good matches from the same live run
("Slaught-O-Matic Platinum Semi-Auto" → "Slaught-O-Matic Platinum" is a contiguous prefix) and both
bad ones — it keeps all five and rejects both.

**Shipped as the contiguous-run rule**, chosen by measuring five candidate rules against the real
corpus (six Apartment Cats files plus three known-good matches from the same live run) rather than by
reasoning. Two things the measurement caught that argument would not have:

- **A plural fold alone looked best and is a coin flip.** It scored 7/9 with the true owner and a
  wrong sibling TIED at 0.60 — it only "won" because the right answer happened to sit first in the
  candidate array. Reversing the candidates flipped it to the wrong mod. Every rule is now checked
  against reversed candidate order, and an order-dependent answer counts as a failure.
- **The last gap was an asymmetry, not the rule.** `CleanModName` filters short all-caps tokens out
  of the QUERY but nothing filtered them out of candidate TITLES, so "Slaught-O-Matic Platinum" could
  never align with a query already missing that "O". Both sides go through the same cleaning now.

Final: **10/10 correct, 0 wrong, 0 missed, 0 order-dependent**, including an adversarial case where
no candidate owns the file. The rule is checked BEFORE Jaccard (a spelled-out title is the strongest
evidence available), requires at least two words, and refuses on a tie — two titles spelled out
equally well is not evidence, and guessing there is the wrong-attach this repo fears most.


## E. Agent access - let an agent verify the smoke and drive the app

Este, 2026-08-17: *"I want both a desktop agent and an IDE/CLI agent to be able to fully verify our
smoke test and use the app (within reason)."*

The framing, the tool surface and the ten safety laws already exist, and they are good —
`docs/superpowers/specs/2026-05-26-agent-access-design-sketch.md`. Read it first; none of it needs
re-deciding. What follows is the gap between that sketch and what is actually built, three months on.

**What shipped:** `src/ModManager.Mcp/` — a stdio MCP server importing Core directly, wired in
`.mcp.json`. Six read tools: `list_games`, `get_active_game`, `list_mods`, `get_mod_context`,
`get_game_shape`, `get_server_info`. It reads the registry off disk and works whether the app is
running or not, exactly as designed.

**What did not:** everything that lets an agent *do* anything. Zero of the sketch's nine write tools,
zero of its three live-app tools, no consent surface, no audit log, and five of its nine planned read
tools (`list_themes`, `list_save_mods`, `list_profiles`, `get_app_settings`, `dry_run_intake`).

The read half answers *what is true*. Este is asking for the other two thirds: *change something*,
and *confirm the app showed it*.

### The principle: parity, plus the reason

Este, 2026-08-17: *"I want the MCP to allow agents to see exactly what a human can see. But some of
those things might matter more to an agent, like the version numbers and whatnot. So I think the
agent would end up seeing more."*

That is the design rule for every read tool, and it is sharper than "expose the data". Two clauses,
and the second is the one that gets skipped:

1. **Parity.** Anything rendered in the UI is readable by an agent. If a human can see a chip, a
   count, a status line, an empty state or a disabled control, so can the agent. A gap here means an
   agent confidently reporting on a screen it cannot actually read.
2. **Plus the reason.** Where the UI renders a *conclusion*, the agent also gets the inputs that
   produced it. Humans read a rendered string; agents diagnose. The agent should see the fields the
   view-model consumed, not just the sentence it printed.

**A10 is the worked example, and it argues for clause 2 better than any principle statement.** A
human looking at that row sees `unknown -> 1.2.1` and can only conclude the app is broken. An agent
under this rule sees that the row was listed because Nexus's per-user flag said so, that no installed
version was ever recorded because the match came from a name search, and that the version pair is
therefore not a comparison at all. Same row, and only one of those two readers can file a correct
bug. A10 sat unfiled for months in plain sight of everyone who opened that screen.

So the read surface is not a serialisation of the UI. It is the UI's inputs *and* its output, with
the link between them intact: what was shown, and what made it show that.

### E1. The write half of the agent surface · `L`

The sketch's own "concrete next step" says: read tools first, *"defer the write tools and the
live-app channel to a second PR after read works end-to-end."* Read works end-to-end. This is that
second PR, and it is the item that makes an agent able to **use** the app.

Take the sketch's catalog as written — `set_mod_enabled`, `apply_loadout`, `intake`, `register_game`,
`apply_theme`, `install_save_mod`, `uninstall_mod`, plus `dry_run_intake` from the read list because
plan-without-writing is how an agent should approach anything destructive. Its ten safety laws are
the acceptance criteria, not decoration. Three deserve restating because they are where this goes
wrong quietly:

- **Reversibility is inherited, not re-implemented.** Every write goes through the same Core paths
  the UI uses. An agent that reaches around them is a second source of truth and a second set of
  bugs.
- **"Within reason" has a hard floor at the ban-risk gate.** `BanRiskRules.ShouldGateEnable` requires
  an explicit human acknowledgement. An agent that can set that acknowledgement itself has deleted
  the law rather than honoured it. The gate must refuse an agent outright and surface a code the
  agent can report back to its human — the human acks in the app, not through the tool.
- **Audit before capability.** The `agent-log.jsonl` in law 8 should land in the *same* PR as the
  first write tool, not after. A write surface with no record of what it did is not something to hand
  someone else's game folder to.

Open question the sketch left and this PR must answer: consent UI shape (law 7). Recommend the
simplest thing that is honest — a first-connect dialog naming the agent, approval for the session,
revocable in Settings. Which lands it squarely in **D1**'s territory, so sequence accordingly.

#### Two audiences, one surface

Este, 2026-08-17: *"I want complete access to the app, especially for development and testing."*

The sketch was written for one audience, a stranger's agent on a stranger's machine, which is why its
framing is a passenger seat. That is right for the shipped product and too tight for the person who
wrote it, working on his own library.

Resolve it as **one tool surface, two consent postures**, never two code paths:

- **Shipped posture.** The sketch's laws as written. Per-session consent, confirm required on
  destructive ops, refusals with codes.
- **Development posture.** Opt in, off by default, never reachable in a Store build. Consent
  pre-granted so a test run is not twenty dialogs, and the confirm-required ops callable directly.

What must NOT differ between them is the audit log and the Core paths taken. A dev posture that
writes through a shortcut is testing something the user will never run, which is worse than not
testing at all.

**The one thing with no dev bypass: the ban-risk gate.** The distinction is the whole reason this
stays safe while still being testable. An agent must be able to *reach* the gate and confirm it
refuses, and must never be able to *satisfy* it. Verifying that the gate blocks is exactly the smoke
case worth automating. An agent that can tick the acknowledgement to get past it has turned the
launcher into the thing this project exists not to be. Same in both postures, permanently.

### E2. An agent has no way to see what the app is showing · `L`

This is the half the sketch never covered, and it is the one with the receipts.

Every one of the four bugs in v0.18.0/v0.18.1 was invisible to everything we own. From the 0.18.1
reviewer letter, written a day ago: *"All four are WinUI code-behind or view-model changes and are
not reachable from the headless test suite."* Specifically — a game was added correctly and the home
did not repaint; the retries that caused produced duplicate registrations; a fresh install hid the
discovery lane; the default theme was wrong. **The MCP could have confirmed the registry was correct
in all four cases and still not caught a single one**, because every one of them was a question about
what the window was showing.

**Feasibility settled 2026-08-17** by a spike against the installed Store build with a real 27-mod
library open: `docs/superpowers/research/2026-08-17-uia-feasibility-spike.md`. Read it before
planning this. Headline: UIA reads and drives the shipped binary out of process, 625 elements in
0.8s, 163 invokable controls, 31 toggles, and `AppStatusText` reads back *"27 of 27 enabled"*. No new
dependency — `UIAutomationClient` loads in stock PowerShell 7.

**Correcting this entry's first draft, which was wrong.** It said the groundwork was "better than
expected" because `AutomationProperties` appear across the XAML. That grep did not separate the two
kinds. The real counts: **92 `AutomationProperties.Name`** (accessibility labels, for screen readers)
and **zero `AutomationProperties.AutomationId`**. Only 8 app-authored ids reach the live tree at all,
via WinUI's partial promotion of `x:Name`, out of 720 `x:Name` attributes in the XAML.

So identification currently rests on **display text**. Every control the harness needs is reachable
today only as `Name='Enable all'`, `Name='+ Game'`, `Name='626 Labs'` — UI copy, in a repo whose
conventions treat microcopy as something to keep improving. Rename a button and a green suite goes
red for no behavioural reason; move one and it keeps passing. Searching for `Play` returns a `Text`
element rather than the button, so a naive harness asserts against a label and never touches the
control.

That makes E2 a bigger job than first written, and the extra work is an **`AutomationId` pass**, not
harness plumbing. Scope it to what the triaged smoke list actually touches rather than all 720
`x:Name`s — which is another reason E3 goes first.

What to build:

- A UIA-backed harness that can enumerate the visible tree, read text and visibility, and invoke
  controls — out of process, so it tests the shipped binary rather than a test host.
- An audit of `AutomationId` coverage. The properties present were added for accessibility, not for
  automation, so assume gaps exactly where the interesting state lives (the discovery lane, the
  status line, the theme picker's selection).
- A bridge so both consumers reach it: the IDE/CLI agent through MCP tools, and a desktop agent
  driving the live app.

Worth saying plainly: this is the difference between *"the registry says the game is added"* and
*"the user can see the game."* Only the second one is the smoke test.

Parity applies here at its strictest, and the awkward case is the one that matters. **Absence has to
be readable.** Three of the four bugs in 0.18 were about something not being on screen: a lane that
should have appeared, a list that did not repaint, a state that never changed. A harness that can
only assert on what exists will pass all three. It has to answer "is the discovery lane visible" with
a real no, rather than with a lookup that finds nothing and shrugs.

#### Screenshots are a verification channel, not a debugging aid

Este, 2026-08-17: *"I do think there are opportunities where an agent can verify something looks
right. Screenshots during an automated UI test give the agent a view. You have caught things in
screenshots without direction."*

This is the correction to an earlier draft of E3, which parked "does it look right" in a human-only
bucket. That was wrong, and the evidence against it was produced in the same session that wrote it:
**A10 and A11 were both found by reading a screenshot**, while checking whether a Store listing image
was flattering. Nobody asked for a bug hunt. Both defects had been on that screen for months, in
front of everyone who opened it.

So the harness has two channels, and they fail in opposite directions:

- **Assertions over the UIA tree.** Deterministic, cheap, diffable, CI-safe. Catches exactly what
  someone thought to assert, and nothing else.
- **A screenshot read back to an agent.** Open ended. Catches what nobody thought to assert, which is
  the entire category that gets shipped. This is how twenty rows reading `unknown` and one mod
  appearing four times get noticed, because neither is a failing assertion anywhere. Every case in
  the harness should capture, whether or not it asserts on the image.

Worth keeping both honest about their limits. A screenshot read is not deterministic and must never
be a CI gate on its own; its output is a *finding to triage*, the same status as a review comment. And
where a property can be measured, measure it: the navy-versus-Forge check in this session came down
to sampling mean RGB across the frame and comparing the blue-minus-red delta against the old set,
which is a stronger claim than any description of a colour. Assert the measurable, capture
everything, and let the agent read what it was not told to look for.

Groundwork already exists: `scripts/capture-store-screenshots.ps1` does DPI-aware, exactly-sized
window capture and knows the WinUI 3 trap that PrintWindow returns blank against GPU composition.
Reuse it rather than writing a second capture path.

### E3. The smoke checklist is not something an agent can execute · `M`

`docs/smoke-tests/pending.md` is around sixty sections and 262 checkbox lines accumulated from late
May to now, in prose, ordered by the PR that produced them, with `merged YYYY-MM-DD` placeholders
that were never filled.

**The work is mostly done; the file does not know it.** Este, 2026-08-17: *"I've done most of that
smoke list, I just haven't checked it off."* So the unchecked boxes are not a backlog of untested
behaviour, they are a record-keeping debt, and the two ways to misread the file point in opposite
directions. Read it as a to-do list and you re-test things that were fine months ago. Read it as
history and you ship believing something was covered when it was not. Neither an agent nor a person
in a hurry can currently tell which box is which.

Three jobs, in order:

1. **Triage with Este.** He is the only one who knows what was actually exercised. Walk it with him
   and mark each entry verified, still pending, or obsolete because the surface no longer exists.
   Cheap, and nothing after it is trustworthy without it.
2. **Convert what survives** into a machine-readable artifact: preconditions, action, observable
   expectation, current status, and the release it was last verified against.
3. **Mark what an agent cannot do, explicitly.** That bucket is smaller than it first looks. It is
   not "anything visual", because a captured screenshot read back to an agent covers most of that
   (see E2). What genuinely remains is the cases needing something outside the app: a real game
   launched and played, a real Nexus download against a live account, hardware behaviour. Those stay
   human-only and stay *named*.

That third job is load-bearing. Este's goal is *"to achieve most of that smoke list with an agent"*,
most rather than all, and the gap has to stay legible. A harness that runs the automatable 80%,
reports all green, and says nothing about the other 20% manufactures precisely the false confidence
this repo distrusts elsewhere. It is the cherry-picked denominator wearing a different hat. Every run
summary should read *"N verified, M require a human, here is M."*

Do this **before** E1 and E2 rather than after. It is the specification those two are built to
satisfy, and writing a harness against a target that has not been defined is how you get a harness
that tests what was easy to test.

### Sequencing

E3 -> E2 -> E1. Define what verification means, build the ability to observe, then grant the ability
to act - so that the first agent write lands in a world where its effect can be seen and recorded.
D1 (Settings) intersects at E1's consent surface and E2's verification of it; take D1's design pass
with E1's consent dialog in scope rather than redesigning Settings twice.
