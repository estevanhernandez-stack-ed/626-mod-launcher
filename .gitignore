# .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user

# Visual Studio / Rider
.vs/
.idea/
*.suo

# test + coverage
[Tt]est[Rr]esults/
*.trx
coverage*.json
coverage*.xml

# secrets — never commit (the CurseForge key lives only in the Worker)
*.secret
.env
appsettings.*.local.json
dist/

# MCP wiring carries a live dashboard bearer token — never commit. Each machine supplies its own.
.mcp.json

# Local Claude Code overrides (machine-specific) — the shared config is committed, this isn't.
.claude/settings.local.json

# Transient superpowers session state (brainstorm scratch, server markers) — not project config.
.superpowers/
